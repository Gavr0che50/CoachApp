# Data layer task

Read `docs/04_DATA_MODEL.md` and the `core` module.

Build the first local data layer:

- workout sessions
- completed sets
- body snapshots
- repositories
- mapping tests

Keep the domain module free of database dependencies.
